---
import Meta from '@components/Meta'
import Header from '@components/header/Header'
import Footer from '@components/footer/footer'
const isIOSBuild = process.env.IOS_BUILD === 'true';import ToastHost from '@components/toast/ToastHost'

import '@styles/global.css'
interface Props {
    title: string
    header?: 'standard' | 'workshop';
};
const { title, header='standard' } = Astro.props as Props;
---
<html lang="en">
    <head>
        <Meta title={title} />
    </head>
    <body>
        {!isIOSBuild && <Header type={header} client:load />}
        
        
        <slot />
        <ToastHost client:only="react" />
    </body>
    {!isIOSBuild && <Footer />}
</html>