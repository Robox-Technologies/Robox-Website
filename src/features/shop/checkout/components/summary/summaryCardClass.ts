/**
 * Shared so the address step's summary, the session summary and the loading
 * skeleton are the same width. They swap places as the checkout advances, and a
 * difference between them resizes the row mid-flow.
 */
export const SUMMARY_CARD_CLASS = 'w-full min-[900px]:w-96 min-[900px]:shrink-0'
