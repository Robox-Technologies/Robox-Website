---
// import step1Image from '@/images/boxToBot/step1.svg';
// import step2Image from '@/images/boxToBot/step2.svg';
// import step3Image from '@/images/boxToBot/step3.svg';
// import step4Image from '@/images/boxToBot/step4.svg';
import Step from './Step';

const steps = [
    {
        stepNumber: 1,
        title: "Unbox Your Kit",
        description: "Open your Ro/Box kit and take out all the components. Make sure you have everything you need before starting.",
        // image: step1Image,
    },
    {
        stepNumber: 2,
        title: "Assemble the Robot",
        description: "Follow the step-by-step instructions to assemble your robot. Take your time and ensure all parts are securely connected.",
        // image: step2Image,
    },
    {
        stepNumber: 3,
        title: "Connect to the App",
        description: "Download the Ro/Box app on your device and connect it to your robot via Bluetooth. This will allow you to program and control your robot.",
        // image: step3Image,
    },
    {
        stepNumber: 4,
        title: "Start Coding",
        description: "Use the app's coding interface to start programming your robot. Experiment with different commands and watch your robot come to life!",
        // image: step4Image,
    },
];
import Article from "@/components/article/Article"
import ArticleText from "@/components/article/ArticleText"
import Line from "@/images/boxToBot/lineStart.svg"
---
<script>
    // let currentImgIndex = 0;
    // function updateBoxToBot() {
    //     const steps = document.querySelectorAll<HTMLDivElement>("div.step");
    //     const imageEl = document.querySelector<HTMLImageElement>("img.boxToBotImg");
    //     if (!imageEl || steps.length === 0) return;

    //     let imgIndex = 0;

    //     for (let i = 0; i < steps.length - 1; i++) {
    //         const step = steps[i];
    //         const nextStep = steps[i + 1];

    //         const midpoint =(step.getBoundingClientRect().bottom + nextStep.getBoundingClientRect().top) / 2;

    //         if (midpoint > window.innerHeight / 2) break;
    //         imgIndex++;
    //     }

    //     if (imgIndex === currentImgIndex) return;

    //     const activeStep = steps[imgIndex];
    //     const image = activeStep?.dataset.image;
    //     if (!image) return;

    //     imageEl.src = image;

    //     imageEl.classList.remove("scale-animate");
    //     void imageEl.offsetWidth;
    //     imageEl.classList.add("scale-animate");

    //     currentImgIndex = imgIndex;
    // }

    // document.addEventListener("scroll", updateBoxToBot);
    // updateBoxToBot();
</script>
<div class="h-fit w-full flex flex-col">
    <div class="bg-blue relative">
        <div>
            <div class="absolute left-10 h-full flex flex-col items-center">
                <Line class="h-full w-full"/>
    
            </div>
            <Article>
                <section>
                    <div class="flex flex-col items-center">
                        <ArticleText className="bg-blue ">
                            <h1 class="text-6xl text-white">Box-to-Bot in 4 Steps</h1>
                            <p class="text-xl text-center leading-[150%] text-white">
                                We know how difficult robotics can be.<br>
                                Its why we've made Ro/Box easy to follow for all ages.
                            </p>
                        </ArticleText>
                    </div>
                </section>
                <section>
                    
                </section>
            </Article>

        </div>
    </div>
    <div class="flex justify-start">
        <div class="absolute left-52.25">
            <hr class="w-5.25 h-100 bg-blue border-0"/>
        </div>
        <div class="w-full relative">
            {steps.map((step) => (
                <Step
                    stepNumber={step.stepNumber} 
                    title={step.title} 
                    description={step.description}
                />
            ))}
        </div>
    </div>
</div>
<!-- <article id="box-to-bot" class="bg-[#23344b]">
    <section class="flex flex-col w-full">
        <div class="intro container py-8">
            <h1 class="text-6xl text-white font-bold mb-8 text-center">
                Box-to-Bot in 4 Steps
            </h1>
            <p class="text-xl text-white mb-12 text-center">
                We know how difficult robotics can be.<br>
                Its why we've made Ro/Box easy to follow for all ages.
            </p>
        </div>
        <div class="columns flex flex-col items-start lg:flex-row gap-8 w-full flex-1">
            <div class="steps flex flex-col gap-12 lg:w-1/2 px-4 pb-8">
                {steps.map((step) => (
                    <Step
                        stepNumber={step.stepNumber} 
                        title={step.title} 
                        description={step.description}
                        image={step.image} 
                    />
                ))}
            </div>
            <img src={step1Image.src} alt="Box to Bot Steps Illustration" class="boxToBotImg w-[300px] sticky top-0 object-contain pt-[calc(max(45vh,250px)-55px)] ml-auto hidden lg:block"/>
        </div>
    </section>
</article> -->